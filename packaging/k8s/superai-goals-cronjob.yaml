# Generated by SuperAI k8s_cronjob — do not hand-edit without regenerating
# superai daemon k8s-render
---
apiVersion: v1
kind: Namespace
metadata:
  name: superai
  labels:
    app.kubernetes.io/name: superai
    app.kubernetes.io/component: goals-daemon
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: superai
  namespace: superai
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: superai-goals-tick
  namespace: superai
  labels:
    app.kubernetes.io/name: superai
    app.kubernetes.io/component: goals-cronjob
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  suspend: false
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        metadata:
          labels:
            app.kubernetes.io/name: superai
            app.kubernetes.io/component: goals-tick
        spec:
          serviceAccountName: superai
          restartPolicy: Never
          containers:
          - name: superai-tick
            image: python:3.12-slim
            imagePullPolicy: IfNotPresent
            command: ["sh", "-c", "pip install -q superai 2>/dev/null || true; python -m pip install -q -e /app 2>/dev/null || true; superai daemon tick --max-goals 2 --no-notify"]
            - name: MEMPALACE_MCP_ALLOW_PEER_WRITER
              value: "1"
            - name: SUPERAI_MOCK_MODE
              value: "1"
            - name: SUPERAI_NON_INTERACTIVE
              value: "1"

resources:
  requests:
    cpu: "50m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"
